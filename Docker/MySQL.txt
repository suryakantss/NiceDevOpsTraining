docker run -d \
  --name mysql-container \
  -p 3306:3306 \
  -v ~/mysql_data:/var/lib/mysql \
  -e MYSQL_ROOT_PASSWORD='root' \
  -e MYSQL_DATABASE='nicedb' \
  mysql


docker exec -it mysql-container mysql -u root -p